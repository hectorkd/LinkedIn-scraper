import React from "react";

// import { ParserMessageResponse } from "types";
import { Typography } from "ui";

// type ExperiencesInformationProps = {
//   experiences: ParserMessageResponse["experiences"] | undefined;
// };

const ExperiencesInformation = (): JSX.Element => {
  return <Typography>Experiences:</Typography>;
};

export default ExperiencesInformation;
